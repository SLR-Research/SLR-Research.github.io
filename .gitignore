.DS_Store
.vscode/
.idea/
*.log
node_modules/
.env
.env.local

# Compiled CSS — generated by the Tailwind build (npm run build:css).
# CI rebuilds it on every deploy, so it is not committed.
assets/css/app.css
